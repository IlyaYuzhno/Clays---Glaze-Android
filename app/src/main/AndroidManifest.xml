<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ilyad.claysglaze">

    <uses-permission android:name="android.permission.INTERNET" />
    <!--
    Allows Glide to monitor connectivity status and restart failed requests if users go from a
    a disconnected to a connected network state.
    -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ClaysGlaze">
        <activity android:name=".ClaysForGlazesActivity" />
        <activity android:name=".GlazeTemperatureActivity" />
        <activity android:name=".MainGlazesActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".FullScreenImage" />
        <activity android:name=".FirebaseStorageActivity" />
        <activity android:name=".InformationActivity" />
        <activity android:name=".CrackleListViewActivity" />
        <activity android:name=".GlazesForClayActivity" />
        <activity android:name=".ClayTemperatureActivity" />
        <activity
            android:name=".FirstActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar.FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>